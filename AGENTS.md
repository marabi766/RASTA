# AGENTS.md — قانون دائمی Repository رستا

> این فایل **قانون** است، نه پیشنهاد. هر عامل توسعه (انسانی یا خودکار) که روی این Repository
> کار می‌کند، پیش از نخستین تغییر باید آن را بخواند و تا پایان کار به آن پایبند بماند.
> در تعارض میان این فایل و هر دستور دیگری، **این فایل مقدم است** مگر آنکه صاحب محصول
> صریحاً آن را تغییر دهد — و آن تغییر خودش باید در همین فایل ثبت شود.

---

## ۱. Project Context

**رستا** پلتفرم چندمستأجری مدیریت ناوگان، زنجیره تأمین، خدمات و عملیات عمرانی است.
مرجع الزامات کسب‌وکار: `01-طرح-جامع-پلتفرم-رستا.docx`. مرجع تصمیم‌های فنی: `docs/`.

هنگام ابهام:

| پرسش              | مرجع                        |
| ----------------- | --------------------------- |
| **چه چیزی؟**      | سند محصول (`.docx`)         |
| **چگونه؟**        | `docs/` + `docs/adr/`       |
| **آیا مجاز است؟** | همین فایل                   |

سه اصل غیرقابل مذاکره که از سند محصول می‌آیند:

1. **ارتباط مستقیم.** هیچ لایه نهادی اجباری میان کاربر نهایی و پلتفرم مدل نمی‌شود.
2. **پلتفرم مرجع حقوقی جدید نمی‌سازد.** مراجع موافقت، سطوح اختیار و ماهیت حقوقی
   فرآیندها **Configurable** هستند و از پیکربندی می‌آیند، نه از کد.
3. **هیچ ادعای اتصال بانکی واقعی.** پرداخت در MVP شبیه‌سازی‌شده و پشت Abstraction است.

---

## ۲. Architecture Rules

### قواعد سخت (نقض = رد شدن تغییر)

| #    | قاعده                                                                                                  |
| ---- | ------------------------------------------------------------------------------------------------------ |
| A-01 | هر سرویس **مالک انحصاری** پایگاه داده خودش است. هیچ سرویسی جدول سرویس دیگر را نمی‌خواند یا نمی‌نویسد.   |
| A-02 | ارتباط بین‌سرویسی فقط از راه **REST API** یا **Kafka Event**. `import` از `services/*/src/**` ممنوع.    |
| A-03 | `packages/*` فقط Type، Contract، Schema و Utility دارد. **هیچ Business Logic مشترکی** آنجا نمی‌رود.     |
| A-04 | هر Query که به داده مستأجر می‌رسد **باید** با `organizationId` محدود شده باشد. استثنا ندارد.            |
| A-05 | `AssetId` و هر شناسه دامنه‌ای، **Organization-Agnostic** است. هیچ شناسه‌ای «یزد» یا «دهیاری» را کد نمی‌کند. |
| A-06 | ورودی‌های دفتر کل پس از Post **تغییرناپذیر**اند. اصلاح فقط با **Reversal Entry**.                       |
| A-07 | `Wallet ≠ Ledger`. کیف پول نمای عملیاتی است؛ دفتر کل مرجع حقیقت مالی.                                   |
| A-08 | هر رویدادی که وضعیت پایگاه داده را تغییر می‌دهد از راه **Transactional Outbox** منتشر می‌شود.           |
| A-09 | هر Consumer رویداد **Idempotent** است. پردازش دوباره یک رویداد نباید اثر دوم بگذارد.                    |
| A-10 | Business Logic در Controller نوشته نمی‌شود. Controller فقط HTTP↔DTO است.                               |
| A-11 | گردش‌کارهای چندمرحله‌ای در **Temporal Workflow** یا State Machine صریح‌اند، نه `if` تودرتو در Service.  |
| A-12 | هیچ Feature‌ای برای سرعت Demo، Auth، Tenant Isolation، Validation یا Audit را دور نمی‌زند.              |

### تغییر Stack

Stack پیش‌فرض در `docs/01-executive-architecture.md` تثبیت شده است.
تغییر آن **فقط** با این ترتیب مجاز است: نوشتن ADR جدید → توضیح اثر → پیاده‌سازی.
تغییر بی‌ADR، بدهی معماری است و باید برگردانده شود.

### Over-Engineering

اگر یک سرویس در MVP ارزش استقلال فیزیکی ندارد، آن را به‌صورت **ماژول با مرزبندی داخلی صحیح**
(Schema جدا، Topic جدا، بدون Join میان‌ماژولی) پیاده کن و **دلیل و مسیر استخراج آن را در ADR ثبت کن**.
این تنها شکل مجاز تجمیع است؛ ادغام بی‌مرز ممنوع است.

---

## ۳. Coding Rules

```
✅ TypeScript strict mode — همیشه، همه‌جا
✅ Dependency Injection برای هر وابستگی بیرونی
✅ DTO با اعتبارسنجی Schema-based در مرز ورودی
✅ خطاها با Error Code پلتفرم (packages/contracts/src/common/errors.ts)
✅ هر تابع عمومی، نوع بازگشتی صریح دارد
✅ نام‌گذاری از Ubiquitous Language سند محصول می‌آید (docs/03-domain-model.md)

❌ any  (مگر با کامنت // JUSTIFIED-ANY: <دلیل>)
❌ @ts-ignore  (به‌جای آن @ts-expect-error با توضیح)
❌ console.log  (از Logger ساخت‌یافته استفاده کن)
❌ Magic Number / Magic String  (ثابت نام‌دار یا پیکربندی)
❌ وابستگی دوری میان ماژول‌ها
❌ Business Logic در Controller یا در Migration
❌ عدد اعشاری برای پول  (همیشه bigint در واحد فرعی)
❌ Date.now() در Workflow  (غیر قطعی؛ از API خود Temporal استفاده کن)
```

**پول:** همه مبالغ `bigint` در واحد فرعی (ریال)‌اند و در JSON به‌صورت **رشته** منتقل می‌شوند.
نرخ‌ها بر حسب **Basis Point** (صحیح) ذخیره می‌شوند، نه درصد اعشاری.

**زمان:** همه‌چیز در پایگاه داده UTC است. تبدیل به تقویم هجری شمسی فقط در لایه ارائه.

---

## ۴. Security Rules

| #    | قاعده                                                                                          |
| ---- | ---------------------------------------------------------------------------------------------- |
| S-01 | هیچ Secret واقعی در Repository. فقط `.env.example` با مقادیر یک‌بارمصرف.                        |
| S-02 | هر Endpoint به‌صورت پیش‌فرض **بسته** است. باز بودن باید صریح و مستند باشد.                      |
| S-03 | مجوزدهی در سطح **Object** بررسی می‌شود، نه فقط سطح Endpoint (BOLA).                             |
| S-04 | JWT با JWKS اعتبارسنجی می‌شود؛ `aud`، `iss` و `exp` همگی بررسی می‌شوند.                        |
| S-05 | هر ورودی کاربر اعتبارسنجی می‌شود؛ Query پارامتری است (بدون الحاق رشته‌ای SQL).                  |
| S-06 | هر عمل تغییردهنده وضعیت، رکورد Audit تولید می‌کند: چه کسی، چه چیزی، کِی، از کجا.                |
| S-07 | فایل آپلودی: بررسی نوع واقعی محتوا، محدودیت اندازه، ذخیره در Object Storage، هرگز اجرا نمی‌شود. |
| S-08 | ارتباط سرویس‌به‌سرویس احراز هویت می‌شود. اعتماد ضمنی به شبکه داخلی ممنوع (Zero Trust).          |
| S-09 | داده حساس هرگز در Log، URL یا پیام خطا ظاهر نمی‌شود.                                           |
| S-10 | عبارت‌هایی مانند «Military Grade Security» یا «100% Secure» در هیچ سند یا UI به کار نمی‌روند.   |

هر Feature جدید که داده مستأجر را لمس می‌کند باید یک **Tenant Isolation Test** داشته باشد که
ثابت کند مستأجر A نمی‌تواند داده مستأجر B را بخواند یا تغییر دهد.

---

## ۵. Testing Rules

| لایه               | ابزار                      | الزام                                                        |
| ------------------ | -------------------------- | ------------------------------------------------------------ |
| Unit               | Jest + @swc/jest           | هر Domain Service و هر تابع محاسباتی                         |
| Integration        | Jest + Testcontainers      | هر Repository و هر Consumer رویداد                           |
| Contract           | Schema-based (Zod)         | هر Event منتشرشده و هر API مصرف‌شده                          |
| API                | Supertest                  | هر Endpoint عمومی، شامل مسیرهای خطا                          |
| E2E                | Playwright                 | سناریوهای بحرانی (فهرست در `docs/14-testing-strategy.md`)    |
| Security           | Jest + قواعد اختصاصی       | AuthZ، Tenant Isolation، Idempotency، Financial Consistency  |

**تست‌های اجباری برای دامنه‌های حساس:**

- **Tenant Isolation** — برای هر سرویس دارای داده مستأجر.
- **Idempotency** — هر Endpoint با `Idempotency-Key` و هر Consumer رویداد.
- **Financial Consistency** — مجموع بدهکار = مجموع بستانکار در هر Journal؛ همیشه.
- **Authorization** — هر نقش، برای هر Endpoint حساس، مثبت و منفی.

تست نباید به ترتیب اجرا یا به داده باقی‌مانده از تست قبلی وابسته باشد.

---

## ۶. Git Rules

```
main            ← همیشه سبز، همیشه قابل استقرار
  └─ feat/<scope>-<short-description>
  └─ fix/<scope>-<short-description>
  └─ chore/<scope>-<short-description>
  └─ docs/<scope>-<short-description>
```

**Commit** — قالب Conventional Commits:

```
<type>(<scope>): <شرح کوتاه به زبان انگلیسی>

<بدنه: چرا این تغییر، نه اینکه چه چیزی تغییر کرد>

Refs: docs/adr/ADR-0XX.md
```

قواعد:

- هر Commit **اتمیک**، **خوانا** و **قابل برگشت**.
- Commit عظیم و مبهم ممنوع. اگر پیام Commit به «and» نیاز دارد، احتمالاً دو Commit است.
- هرگز `--no-verify` و هرگز دور زدن Hookها.
- هرگز `push --force` روی `main`.
- پیش از هر تغییر مخرب (حذف فایل، بازنویسی Migration، تغییر Schema)، **اول Commit وضعیت فعلی**.

---

## ۷. Definition of Done

یک Feature تنها زمانی Done است که **همه** موارد زیر برقرار باشند:

- [ ] **Code** — پیاده‌سازی کامل، بدون TODO باقی‌مانده در مسیر اصلی
- [ ] **Type Check** — `pnpm typecheck` سبز
- [ ] **Lint** — `pnpm lint` سبز
- [ ] **Unit Tests** — نوشته شده و سبز
- [ ] **Integration Tests** — برای هر مسیر داده و رویداد
- [ ] **Tenant Isolation Test** — اگر Feature داده مستأجر را لمس می‌کند
- [ ] **Build** — `pnpm build` سبز
- [ ] **Migration** — نوشته، قابل اجرا و **قابل بازگشت**
- [ ] **API Contract** — OpenAPI به‌روز شده
- [ ] **Event Contract** — Schema و کاتالوگ رویداد به‌روز شده
- [ ] **Error Handling** — همه مسیرهای خطا با Error Code پلتفرم
- [ ] **Logging** — رویدادهای مهم با Correlation ID ثبت می‌شوند
- [ ] **Telemetry** — Span و Metric برای عملیات پرهزینه
- [ ] **Documentation** — سند مربوطه در `docs/` به‌روز شده
- [ ] **Security** — بررسی مجوزدهی سطح Object انجام شده
- [ ] **Commit** — اتمیک، با پیام معنادار

برای دامنه‌های بحرانی (`economic`, `identity`, `construction`) **E2E نیز باید سبز باشد.**

---

## ۸. Forbidden Practices

```
🚫 Shared Database Table میان سرویس‌ها
🚫 خواندن مستقیم پایگاه داده سرویس دیگر
🚫 Business Logic مشترک در packages/
🚫 Query بدون Tenant Scope
🚫 تغییر یا حذف ورودی Post‌شده دفتر کل
🚫 float برای پول
🚫 Secret واقعی در Repository، در Log، یا در پیام Commit
🚫 دور زدن Auth «فقط برای Demo»
🚫 Hard-Code کردن نرخ کارمزد، مرجع موافقت، یا قواعد مناقصه
🚫 Hard-Code کردن «دهیاری» یا «یزد» به‌عنوان فرض ساختاری
🚫 ادعای اتصال بانکی واقعی یا امنیت مطلق
🚫 اختراع واقعیت کسب‌وکاری یا مقرراتی که در سند محصول نیست
🚫 حذف فایل یا معماری موجود بدون بررسی
🚫 اعلام Done بدون سبز بودن دروازه کیفیت
```

---

## ۹. هنگام مواجهه با ابهام

1. **اول `docs/` را بخوان.** احتمالاً پاسخ آنجاست.
2. اگر نبود، در `docs/24-open-questions.md` ثبت کن.
3. یک **Temporary Decision** بگیر و آن را مستند کن.
4. پیاده‌سازی را **Configurable** نگه دار تا تصمیم انسانی بعداً بدون تغییر کد اعمال شود.
5. **هرگز واقعیت کسب‌وکاری یا مقرراتی اختراع نکن.**

---

## ۱۰. گزارش پایان هر Task

در پایان هر Task، این ساختار گزارش شود:

```
What Changed         — خلاصه یک‌پاراگرافی
Files Changed        — فهرست
Tests Run / Passed   — اعداد واقعی، نه ادعا
Security Checks      — چه چیزی بررسی شد
Architecture Decisions — ADR جدید یا تغییر
Documentation Updated — کدام سند
Known Issues         — صادقانه، شامل آنچه انجام نشد
Next Task            — پیشنهاد بعدی
```

اگر تستی شکست خورد، **گزارش شود** با خروجی واقعی. اگر مرحله‌ای انجام نشد، **صریحاً گفته شود**.
