/**
 * The portal's entry route.
 *
 * It is deliberately a placeholder and says so on screen. The screens of
 * `EXP-002` through `EXP-004` are not part of this story, and a page that
 * looked finished while nothing behind it worked would be the kind of claim
 * this repository does not make.
 */
export default function HomePage() {
  return (
    <main>
      <h1>رستا</h1>
      <p>
        پایهٔ پورتال برپا شده است. صفحه‌های دامنه‌ای هنوز ساخته نشده‌اند و در داستان‌های بعدی
        می‌آیند.
      </p>
    </main>
  );
}
