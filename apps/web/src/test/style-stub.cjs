// Jest has no CSS parser. Components are asserted on behaviour, roles and
// accessible names — never on the stylesheet — so a stylesheet import resolves
// to nothing rather than failing the module graph.
module.exports = {};
